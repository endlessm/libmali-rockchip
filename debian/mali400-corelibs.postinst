#!/bin/sh

set -e

THIS_PACKAGE=mali400-corelibs
THIS_SCRIPT=postinst

case "$1" in
  configure)
  # Activate the trigger selecting Mali400 as GLX provider
  dpkg-trigger --no-await register-glx-alternative-mali400
esac

#DEBHELPER#
